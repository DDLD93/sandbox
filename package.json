{
  "name": "fetch-download-service",
  "version": "1.1.0",
  "description": "Paste a URL in the browser to download a file, or POST a URL to the API and get the file streamed back. Plus an audio transcriber section (chunk + OpenRouter STT, jobs in Postgres, artifacts in S3-compatible storage).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node transcribe/migrate.js",
    "db:reset": "node transcribe/reset.js"
  },
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1053.0",
    "@aws-sdk/lib-storage": "^3.1053.0",
    "@openrouter/sdk": "^0.1.0",
    "dotenv": "^17.4.2",
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "pg": "^8.11.5"
  }
}
